# MongoDB connection (override with SPRING_DATA_MONGODB_URI env var if needed)
spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/itemsdb}

# Server configuration
server.port=${PORT:8080}

# Logging
logging.level.org.springframework.web=INFO
